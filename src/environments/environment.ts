export const environment = {
  production: false,
  apiUrl: 'http://localhost:80',
  keycloak: {
    clientId: 'entrena-sync-client',
    realm: 'Entrena-Sync-realm',
    authServerUrl: 'http://localhost:9090/realms/Entrena-Sync-realm',
  }
}
